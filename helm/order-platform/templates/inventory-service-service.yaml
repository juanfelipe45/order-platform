apiVersion: v1
kind: Service
metadata:
  name: inventory-service
spec:
  selector:
    app: inventory-service
  ports:
    - port: {{ .Values.services.inventoryService.port }}
      targetPort: {{ .Values.services.inventoryService.targetPort }}
  type: {{ .Values.services.inventoryService.type }}