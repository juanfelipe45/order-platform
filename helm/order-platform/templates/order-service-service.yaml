apiVersion: v1
kind: Service
metadata:
  name: order-service
spec:
  selector:
    app: order-service
  ports:
    - port: {{ .Values.services.orderService.port }}
      targetPort: {{ .Values.services.orderService.targetPort }}
  type: {{ .Values.services.orderService.type }}